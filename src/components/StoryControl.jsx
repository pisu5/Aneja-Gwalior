import React from 'react';

const StoryControls = () => {
  return (
    <div className="story-controls">
      {/* Add controls such as play/pause, rewind, skip */}
    </div>
  );
};

export default StoryControls;
